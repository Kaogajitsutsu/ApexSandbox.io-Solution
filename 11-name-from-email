public String nameFromEmail(String email) {
    //code here
    String first = email.substringBefore('.');
    String last = email.substringBetween('.', '@');

    String lowerFirst = first.toLowerCase();
    String lowerLast = last.toLowerCase();
    
    Return lowerFirst.capitalize() + ' ' + lowerLast.capitalize();
}
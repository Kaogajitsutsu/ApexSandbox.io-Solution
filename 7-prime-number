public Boolean isPrime(Integer num) {
    //code here
    for (Integer i = 1, i <= (num - 1); i++) {
        if (Math.mod(num, i) == 0) {
            return false;
        } else {
            return true;
        }
    }
}